<%= turbo_stream_from "stories" %>
<h1><%= @story.title %></h1>
<div id="<%= dom_id(@story) %>">
  <%= @story.body %>
</div>
<%= button_to "Delete",
              story_path(@story),
              method: :delete %>
